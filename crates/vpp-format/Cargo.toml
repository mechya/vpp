[package]
name = "vpp-format"
description = "Reads and writes the VPP containers: the .vpp page package, the .vppm update manifest, the code.bin container, and vpp.json site configuration."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]

[lints]
workspace = true
