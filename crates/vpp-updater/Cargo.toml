[package]
name = "vpp-updater"
description = "Keeps visited pages on disk, pins each site's publisher key on first use, checks manifests, and downloads only the resources whose hashes changed."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
vpp-format.workspace = true

[lints]
workspace = true
