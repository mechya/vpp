# Shared platform assets

Source artwork that every platform's icons and images are generated from, so the app looks the same everywhere and there is one file to change.

* `icon.svg`: the VPP application icon (to be added)

Each `platforms/<os>/README.md` says how its icon sizes and formats are produced from here.
